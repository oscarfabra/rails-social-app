<% if @feed_items.any? %>
  <ol class="microposts">
    <%= 
    # micropost partial called coz each element @feed_items has class Micropost
    render @feed_items
    %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>