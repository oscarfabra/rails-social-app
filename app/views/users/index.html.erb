<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
    <%= 
    # Rails automatically iterates over each user calling _user.html.erb 
    # partial on each one
    render @users
    %>
</ul>

<%= will_paginate %>