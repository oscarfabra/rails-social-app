<% if logged_in? %>
  <!-- Display form for creating microposts. -->
  <%= render 'static_pages/user_home_page' %>
<% else %>
  <!-- Display landing page. -->
  <%= render 'static_pages/landing_page' %>
<% end %>